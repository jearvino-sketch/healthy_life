# Repository collaboration rules

- 本规则适用于所有 agent 及其子 agent：任何 agent 每次完成一项仓库改动后，都必须在本次任务结束前提交改动并推送到当前配置的 remote，不得只提交不推送。
- 提交前执行与改动相匹配的检查，只暂存本次任务相关文件，并使用清晰的提交说明。
- 默认将当前分支推送到 `origin`；除非用户明确要求，否则不得 force push。
- 如果提交或推送失败，必须向用户说明失败原因和仍未同步的内容。
